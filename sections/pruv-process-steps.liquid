{{ 'section-pruv-process-steps.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="pruv-process color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding">
  <div class="pruv-process__header page-width page-width--narrow" data-animate="fade-up">
    {%- if section.settings.eyebrow != blank -%}
      <p class="pruv-process__eyebrow">{{ section.settings.eyebrow | escape }}</p>
    {%- endif -%}
    {%- if section.settings.heading != blank -%}
      <h2 class="pruv-process__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}
    {%- if section.settings.text != blank -%}
      <div class="pruv-process__text rte">{{ section.settings.text }}</div>
    {%- endif -%}
  </div>

  {%- if section.blocks.size > 0 -%}
    <div class="pruv-process__list page-width" data-animate="stagger">
      {%- for block in section.blocks -%}
        {%- assign step_number = forloop.index | prepend: '0' | slice: -2, 2 -%}
        <div class="pruv-process__step" {{ block.shopify_attributes }}>
          <span class="pruv-process__number" aria-hidden="true">{{ step_number }}</span>
          <div class="pruv-process__step-content">
            {%- if block.settings.title != blank -%}
              <h3 class="pruv-process__step-title">{{ block.settings.title | escape }}</h3>
            {%- endif -%}
            {%- if block.settings.description != blank -%}
              <p class="pruv-process__step-description">{{ block.settings.description }}</p>
            {%- endif -%}
          </div>
        </div>
      {%- endfor -%}
    </div>
  {%- endif -%}
</div>

{% schema %}
{
  "name": "Prüv process steps",
  "tag": "section",
  "class": "section pruv-process-steps-section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Our Process"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Insight-led. <strong>Intentionally built.</strong>"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Every formula follows the same disciplined path from question to shelf.</p>"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 72
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 72
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Listen first"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "We start by researching the real concerns Indian hair and scalp actually face, before a single ingredient is chosen."
        }
      ]
    }
  ],
  "max_blocks": 8,
  "presets": [
    {
      "name": "Prüv process steps",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "title": "Listen first",
            "description": "We start by researching the real concerns Indian hair and scalp actually face, before a single ingredient is chosen."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Define the challenge",
            "description": "Insights are shaped into a structured brief with clear, measurable performance goals."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Engineer with intent",
            "description": "High-efficacy actives are chosen and combined based on how they actually behave on hair and scalp."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Test for performance",
            "description": "Each formula runs through internal trials and safety assessments before it goes anywhere near a customer."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Validate at scale",
            "description": "Results are confirmed through real usage across different water types, climates and hair types."
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Deliver with precision",
            "description": "Validated formulas are produced under controlled conditions, with the same quality checks on every batch."
          }
        }
      ]
    }
  ]
}
{% endschema %}
